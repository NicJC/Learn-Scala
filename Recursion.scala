package Variables

object Recursion {
  // sorting lists
  extension [A](List: List[A]) {
    def insertSort(Comp: (A, A ) => Int): List[A] = {
      def insert(elem: A, rest: List[A]) = List[A] = ???
    }

    def mergeSort(Comp: (A, A ) => Int): List[A] = ???
    def quickSort(Comp: (A, A ) => Int): List[A] = ???
  }

  def Main(args: Array[String]): Unit = {

  }

}
